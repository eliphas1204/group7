import 'package:flutter/material.dart';

class ViewNotePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('View Note')),
      body: Center(child: Text('Note details here')),
    );
  }
}
